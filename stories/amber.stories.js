import React from "react";
import Display from "./Display";
import { amber as theme } from "../src/index";

export default {
  title: "Amber",
};

export const amber = () => <Display {...{ theme }} />
  
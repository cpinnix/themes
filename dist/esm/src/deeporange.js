import generateTheme from './utils/generateTheme.js';
import { deeporange as deeporange$1, white } from '@cpinnix/colors';

var deeporange = generateTheme({
  name: "deeporange",
  primary: deeporange$1.swatch[500],
  secondary: deeporange$1.swatch[500],
  text: white.swatch[500]
});

export { deeporange as default };
//# sourceMappingURL=deeporange.js.map
